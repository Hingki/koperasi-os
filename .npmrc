# ---------------------------------------------
# Koperasi OS — Standard NPM Configuration
# ---------------------------------------------
# legacy-peer-deps=true
#   Mengizinkan instalasi meskipun ada konflik peer dependencies.
#   Penting untuk ekosistem Next.js + Supabase + Playwright.
#
# strict-peer-deps=false
#   Memastikan npm tidak memblokir instalasi hanya karena
#   ketidaksesuaian peer dependencies.
#
# Catatan:
# - Aman untuk CI/CD dan Codespaces.
# - Tidak mengandung token atau credential.
# - Berlaku untuk seluruh repositori Koperasi OS.
# ---------------------------------------------

legacy-peer-deps=true
strict-peer-deps=false
